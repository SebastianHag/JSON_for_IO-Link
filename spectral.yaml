# Linting rules
extends: [spectral:oas, spectral:asyncapi]
rules:
  path-params: off # to disbale error: path-params  Paths "/devices/{deviceAlias}/parameters/{index}/value" and "/devices/{deviceAlias}/parameters/{parameterName}/value" must not be equivalent.
